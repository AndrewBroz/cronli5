node_modules
npm-*

# Build artifacts (generated by `npm run build`, shipped via the npm package).
dist
cronli5.min.js

# Test coverage output.
coverage
.nyc_output

# Scratch output (review packets, generated tables for human review).
tmp
# Generated type declarations.
types

# Transient git worktrees created by the add-language workflow.
.claude/worktrees
